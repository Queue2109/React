
// const creats a value that cannot be changed
/*
if(true){
    let namen = 'Ryan';
}

alert(name);


let fname = 'Ryan';
let lname = 'D';
let age = prompt("How old is Ryan");

let result = `${fname} ${lname} is ${age} years old`;
alert(result);

// to determine what default values are, you write them in the function

function welcome(user="Mystery person", message="helo") {
    alert(`Hello ${user}, ${message}`);

}
welcome("Ryan", "Good morning");
welcome();


function greeting(message) {
    return alert(`${message} everyone!`);

}

let greeting = (message) => alert(`${message} everyone!`);

greeting("good morning");

let createBlog = (title, body) => {
    if(!title) {
        throw new Error('A title is required');

    }
    if(!body) 
    throw new Error('Body cant be empty');

    return `${title} - ${body}`;
}

createBlog('Blog title', 'blog body');
*/

